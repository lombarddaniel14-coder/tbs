# STATUS

> TBS — Python voice assistant (wake word, STT/TTS, PC control). State: built + compiled; ANTHROPIC_API_KEY set; no confirmed live call yet. Next: voice-stack refresh planned (RealtimeSTT + openWakeWord + piper1-gpl — NOT the dead rhasspy/piper repo). Path constants in core\tools\projects.py.

_Last updated: 
